def histogram(word:str):
    letters = {}
    for letter in word:
        if letter not in letters:
            letters[letter] = ""
        letters[letter] += "*"
    for key, value in letters.items():
        print(f"{key} {value}")



histogram("abba")