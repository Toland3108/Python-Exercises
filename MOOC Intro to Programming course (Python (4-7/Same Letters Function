def same_chars(word, num1, num2):
    if num1 >= len(word) or num2 >= len(word):
        return False
    letter1 = word[num1]
    letter2 = word[num2]
    if letter1 == letter2:
        return True
    else:
        return False

print(same_chars("coder", 10, 1))