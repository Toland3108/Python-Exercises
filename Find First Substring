word_input = input("Please type in a word")
character = input("Please type in a character:")

if character in word_input:
    index = word_input.find(character)
    printed_word = word_input[index : (index + 3)]
    if len(printed_word) == 3:
        print(printed_word)